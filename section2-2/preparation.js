/* function setup() {
  createCanvas(100, 100);
  background(163, 170, 181);
  fill(0);
  for(let i = 0; i < 9; i++){
    line((i + 1) * 10, 0, (i + 1) * 10, 100);
    if(i < 4){
      strokeWeight(2);
    }else{
      strokeWeight(1);
    }
  }
} */

/* function setup() {
  createCanvas(100, 100);
  background(163, 170, 181);
  fill(0);
  for(let i = 0; i < 9; i++){
    line((i + 1) * 10, 0, (i + 1) * 10, 100);
    if(i % 2 == 0){
      strokeWeight(2);
    }
    else{
      strokeWeight(1);
    }
  }
} */

/* function setup() {
  createCanvas(100, 100);
  background(163, 170, 181);
  fill(0);
  for(let i = 0; i < 9; i++){
    line((i + 1) * 10, 0, (i + 1) * 10, 100);
    if(i < 3){
      strokeWeight(1);
    } else if(i < 6){
      strokeWeight(2);
    } else{
      strokeWeight(3);
    }
  }
} */

/* function setup() {
  createCanvas(100, 100);
  background(163, 170, 181);
  fill(0);
  for(let i = 0; i < 9; i++){
    line((i + 1) * 10, 0, (i + 1) * 10, 100);
    if(i % 3 == 0){
      strokeWeight(1);
    } else if (i % 3 == 1){
      strokeWeight(2);
    } else {
      strokeWeight(3);
    }
  }
} */
