// 小手調べ
function setup(){
  for(let i = 0; i < 10; i++){
    // BLANK[1]
  }
}

/* 予習での解答
function setup() {
  createCanvas(220, 220);
  for(let i = 0; i < 10; i++){
    noFill();
    ellipse(110, 110, (i + 1) * 10);
    if(i < 5){
      stroke(0, 0, 255);
    } else {
      stroke(255, 0, 0);
    }
  }
} */
